# plugin
This project implements a path provider for BeTA by querying the Node.js
service contained within this repository. BeTA requires a single .so
file, which can be generated by running
```
cmake .
make
```
This specific example requires the static libraries libcurl and libcurlpp.